This folder contains self-contained jupyter notebooks with the analysis for the paper.

We ran analyses with the algorithm prior to it being wrapped in the API. You can replace the B_nnls and the A used in the notebooks with model.loadings and model.components as appropriate.
